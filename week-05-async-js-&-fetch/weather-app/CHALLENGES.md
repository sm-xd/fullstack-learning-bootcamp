🎯 BONUS CHALLENGES - Extend this app! ( generated by GPT )

1. ADD GEOLOCATION: done
   - Add a "Use My Location" button
   - Use navigator.geolocation API
   - Fetch weather by coordinates

2. ADD 5-DAY FORECAST:
   - Use the forecast endpoint
   - Display forecast below current weather
   - Show icons for each day

3. ADD LOCAL STORAGE:
   - Save last searched city
   - Load it on page refresh
   - Show search history

4. ADD UNIT TOGGLE:
   - Button to switch Celsius/Fahrenheit
   - Store preference in localStorage

5. ADD ERROR RETRY:
   - Implement automatic retry with exponential backoff
   - Show retry countdown to user

6. ADD WEATHER BACKGROUNDS:
   - Change background based on weather condition
   - Different gradients for sunny, rainy, cloudy, etc.

7. ADD ACCESSIBILITY:
   - Proper ARIA labels
   - Keyboard navigation
   - Screen reader support